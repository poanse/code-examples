Script contains class "generator" which generates random sentences with a simple algorithm.

.learn()

Reads txt file (by default speeches.txt which contains a sample of randomly selected Obama speeches) and creates a table with pairs of adjacent words.

.generate()

Generates a random word sequence based on learned text.
